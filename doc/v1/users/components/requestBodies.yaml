# v1/users/components/requestBodies.yaml
CreateUser:
  description: Create user request
  content:
    application/json:
      schema:
        type: object
        properties:
          email:
            $ref: "../components/schemas.yaml#/Email"
          password:
            $ref: "../components/schemas.yaml#/Password"
          role:
            $ref: "../components/schemas.yaml#/UserRole"
